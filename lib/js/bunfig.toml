[install]
# Never auto-install from npm. Use only local node_modules.
# Makes `bunx`/`bun x` behave as if --no-install were always passed.
auto = "disable"

# Only install package versions published at least 10 days ago
minimumReleaseAge = 864000
